<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Dark" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/oil</item>
        <item name="colorPrimaryDark">@color/jet</item>
        <item name="colorAccent">@color/appwhite</item>

        <item name="android:windowBackground">@color/jet</item>

        <item name="colorControlNormal">@color/iron</item>
        <item name="colorControlActivated">@color/appwhite</item>
        <item name="colorControlHighlight">@color/appwhite</item>
        <item name="android:textColorHint">@color/apporange</item>

        <item name="colorButtonNormal">@color/apporange</item>
    </style>

    <style name="AppTheme.Dark.Dialog" parent="Theme.AppCompat.Dialog">
        <item name="colorAccent">@color/black</item>
        <item name="android:textColorPrimary">@color/iron</item>
        <item name="android:background">@color/oil</item>
    </style>

</resources>

